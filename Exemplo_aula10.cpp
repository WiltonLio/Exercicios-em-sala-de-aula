#include <iostream>

using namespace std;

int main(Void)
{
    char nome[100] = 'Wilton Freire';
    // ...
    char usuario[100] = '';
    // ...
    //usuario = nome;
    int i = 0;
    //for (int i = 0; i < nome(i) != '\0'; i++)
    while (nome[i] != '\0')
    {
        usuario[i] = nome[i];
        i++;
    }
        usuario(i) = '\0';

    
}